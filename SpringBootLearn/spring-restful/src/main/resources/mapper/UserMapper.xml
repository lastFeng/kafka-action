<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springboot.springrestful.dao.CityDao">

    <select id="findByCityId" resultType="com.springboot.springrestful.domain.City" parameterType="long">
      select id, province_id, city_name, description
      from city
      where id = #{id}
    </select>
    <select id="findAllCity" resultType="com.springboot.springrestful.domain.City">
      select id, province_id, city_name, description
      from city
    </select>
</mapper>